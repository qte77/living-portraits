.venv/
data/
*.log
*.png
*.mp4
*.gif
_shots/
__pycache__/
pipeline/vendor/
*.done

# article assets are deliberate deliverables — keep them tracked despite *.png above
!_article/assets/
!_article/assets/*.png
.pytest_cache/
.ruff_cache/

# agent session config, not repo content. scripts/release.py:52 already excludes
# .claude from the published subset; this keeps it out of git in the first place.
.claude/

# generated by ci.yaml's security job (`uv export`) to hand pip-audit a
# requirements file scoped to core + dev -- not a source of truth, uv.lock is.
requirements-audit.txt
